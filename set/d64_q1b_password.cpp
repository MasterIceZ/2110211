#include <bits/stdc++.h>

using namespace std;

signed main(int argc, char *argv[]) {
	cin.tie(nullptr)->ios::sync_with_stdio(false);
	int n, m, l;
	cin >> n >> m >> l;
	vector<int> k(l);
	for(auto &x: k) {
		cin >> x;
	}
	
	return 0;
}
